#include <stdio.h>

int main(int argc, char const *argv[])
{
    double a = 20;
    int b = 3.0;
    
    // int result = (double) a / (double) b;

    printf("result: %lf\n", a / b);
    
    return 0;
}
